 jest-dom adds custom jest matchers for asserting on DOM nodes.
 allows you to do things like
 expect(element).toHaveTextContent(reacti)
 learn more httpsgithub.comtesting-libraryjest-dom
import '@testing-libraryjest-dom';